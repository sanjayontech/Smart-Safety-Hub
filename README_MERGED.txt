This package combines all uploaded projects into a single archive.
Projects are kept in separate folders to avoid file conflicts.
Further code integration is required to make them a single unified application.
